{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h2>Cadastro de sobremesas</h2>

<div class="card">
    <div class="card-body">
        <form action="/admin/sobremesas/nova" method="post">
            <label for="nome">Nome da sobremesa:</label>
            <input type="text" id="nome" name="nome" class="form-control" required>

            <label for="descricao">Descrição:</label>
            <textarea name="descricao" id="descricao" cols="30" rows="10" class="form-control" required></textarea>

            <button type="submit" class="btn btn-success btn-block mt-2">Cadastrar sobremesa</button>
        </form>
    </div>
</div>
