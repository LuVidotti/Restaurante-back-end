{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h2>Cadastro de pratos principais</h2>

<div class="card">
    <div class="card-body">
        <form action="/admin/pratos-principais/novo" method="post">
            <label for="nome">Nome do prato:</label>
            <input type="text" id="nome" name="nome" class="form-control" required>

            <label for="descricao">Descrição:</label>
            <textarea name="descricao" id="descricao" cols="30" rows="10" class="form-control" required></textarea>

            <button type="submit" class="btn btn-success btn-block mt-2">Cadastrar prato</button>
        </form>
    </div>
</div>
