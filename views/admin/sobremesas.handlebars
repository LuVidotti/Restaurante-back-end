<h2>Sobremesas:</h2>
<hr>

<a href="/admin/sobremesas/cad"><button class="btn btn-success mb-4">Cadastrar sobremesa</button></a>

{{#each sobremesas}}
    <div class="card mb-4">
        <div class="card-body">
            <h3>Nome: {{nome}}</h3>
            <p>Descrição: {{descricao}}</p>
            <a href="/admin/sobremesas/edit/{{_id}}"><button class="btn btn-dark btn-sm">Editar</button></a>
            <form action="/admin/sobremesas/deletar" method="post">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger btn-sm mt-2">Deletar</button>
            </form>
        </div>
    </div>
{{else}}
    <h4>Nenhuma sobremesa cadastrada</h4>
{{/each}}