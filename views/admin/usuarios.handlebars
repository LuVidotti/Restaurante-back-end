<h2>Lista de usuários:</h2>
<hr>

<a href=""><button class="btn btn-success mb-4">Criar usuário</button></a>

{{#each usuarios}} 
    <div class="card mb-4">
        <div class="card-body">
            <h4>Nome: {{nome}}</h4>
            <h5>E-mail: {{email}}</h5>
            <p>Administrador: {{eAdmin}}</p>
        </div>
    </div>
{{else}}
    <h4>Nenhum usuário cadastrado</h4>
{{/each}}